import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.caravanaflavio.bagagensbarretao',
  appName: 'Bagagens Barret\u00e3o',
  webDir: 'dist',
};

export default config;